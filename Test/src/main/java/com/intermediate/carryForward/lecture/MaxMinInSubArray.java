package com.intermediate.carryForward.lecture;
/*
* Given an array of N integers, return length of smallest subarray which
* contains booth minimum and maximum of the array.
* */
public class MaxMinInSubArray {
    //Brute force approach
    //Find minimum and maximum in array - O(N)
    //Find maximum and minimum in all subarray - O(N^3)
    //Space complexity - O(1)
}
