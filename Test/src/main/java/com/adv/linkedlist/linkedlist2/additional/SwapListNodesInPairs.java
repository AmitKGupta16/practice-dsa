package com.adv.linkedlist.linkedlist2.additional;

import com.adv.linkedlist.ListNode;

public class SwapListNodesInPairs {

    public ListNode swapPairs(ListNode A) {
        ListNode temp = A;
        while(temp !=null && temp.next !=null) {
           // ListNode temp1=

        }
        return A;
    }
}
